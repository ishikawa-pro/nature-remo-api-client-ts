{
    "name": "nature-remo-api-client",
    "version": "0.0.1",
    "description": "NodeJS client for nature-remo-api-client",
    "repository": "GIT_USER_ID/GIT_REPO_ID",
    "main": "dist/api.js",
    "types": "dist/api.d.ts",
    "scripts": {
        "clean": "rm -Rf node_modules/ *.js",
        "build": "tsc",
        "prepare": "rm -rf dist && npm run build",
        "test": "npm run build && node dist/client.js"
    },
    "author": "OpenAPI-Generator Contributors",
    "license": "Unlicense",
    "dependencies": {
        "bluebird": "^3.5.0",
        "request": "^2.81.0",
        "@types/bluebird": "*",
        "@types/request": "*",
        "rewire": "^3.0.2"
    },
    "devDependencies": {
        "typescript": "^2.4.2",
        "@types/node": "8.10.34"
    },
    "files": [
      "dist"
    ]
}
